# Legacy v1

This directory contains the original BallsDeep v1 code preserved for
reference. It is **not** loaded by the published library and has no test
coverage. See the top-level [`PLAN.md`](../PLAN.md) for what was kept,
what was reworked, and why.

The v2 library lives in [`../src`](../src).
